name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create typo3-core-contributions skill package
        run: |
          mkdir -p dist-skill
          cp skills/typo3-core-contributions/SKILL.md dist-skill/
          cp LICENSE dist-skill/
          [ -d "skills/typo3-core-contributions/references" ] && cp -r skills/typo3-core-contributions/references dist-skill/
          [ -d "skills/typo3-core-contributions/scripts" ] && cp -r skills/typo3-core-contributions/scripts dist-skill/
          [ -d "skills/typo3-core-contributions/assets" ] && cp -r skills/typo3-core-contributions/assets dist-skill/
          [ -d "skills/typo3-core-contributions/templates" ] && cp -r skills/typo3-core-contributions/templates dist-skill/
          [ -d "skills/typo3-core-contributions/examples" ] && cp -r skills/typo3-core-contributions/examples dist-skill/
          [ -f "skills/typo3-core-contributions/checkpoints.yaml" ] && cp skills/typo3-core-contributions/checkpoints.yaml dist-skill/
          cd dist-skill
          zip -r ../typo3-core-contributions-skill-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../typo3-core-contributions-skill-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create typo3-core-contributions plugin package
        run: |
          mkdir -p dist-plugin
          # Include typo3-core-contributions skill files
          mkdir -p dist-plugin/skills/typo3-core-contributions
          cp skills/typo3-core-contributions/SKILL.md dist-plugin/skills/typo3-core-contributions/
          [ -d "skills/typo3-core-contributions/references" ] && cp -r skills/typo3-core-contributions/references dist-plugin/skills/typo3-core-contributions/
          [ -d "skills/typo3-core-contributions/scripts" ] && cp -r skills/typo3-core-contributions/scripts dist-plugin/skills/typo3-core-contributions/
          [ -d "skills/typo3-core-contributions/assets" ] && cp -r skills/typo3-core-contributions/assets dist-plugin/skills/typo3-core-contributions/
          [ -d "skills/typo3-core-contributions/templates" ] && cp -r skills/typo3-core-contributions/templates dist-plugin/skills/typo3-core-contributions/
          [ -d "skills/typo3-core-contributions/examples" ] && cp -r skills/typo3-core-contributions/examples dist-plugin/skills/typo3-core-contributions/
          [ -f "skills/typo3-core-contributions/checkpoints.yaml" ] && cp skills/typo3-core-contributions/checkpoints.yaml dist-plugin/skills/typo3-core-contributions/
          cp LICENSE dist-plugin/
          # Include plugin manifest
          [ -d ".claude-plugin" ] && cp -r .claude-plugin dist-plugin/
          cd dist-plugin
          zip -r ../typo3-core-contributions-plugin-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../typo3-core-contributions-plugin-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          files: |
            typo3-core-contributions-skill-${{ steps.version.outputs.version }}.zip
            typo3-core-contributions-skill-${{ steps.version.outputs.version }}.tar.gz
            typo3-core-contributions-plugin-${{ steps.version.outputs.version }}.zip
            typo3-core-contributions-plugin-${{ steps.version.outputs.version }}.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
